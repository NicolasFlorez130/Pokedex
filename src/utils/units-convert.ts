export const dmToCm = (dm: number) => dm * 10;

export const hgToKg = (hg: number) => hg / 10;

export const dmToIn = (dm: number) => dm * 3.937;

export const hgToLb = (hg: number) => hg / 4.5359237;
